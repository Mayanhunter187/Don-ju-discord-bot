version: '3.8'

services:
  discord-music-bot:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      - ..:/app
    # Keep the container running
    command: sleep infinity
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
